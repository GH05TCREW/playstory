# Copy to .env and set your key
OPENAI_API_KEY=sk-...
# Optional: override API base if needed
# OPENAI_API_BASE=https://api.openai.com

# Defaults (can be changed per request)
SORA_MODEL=sora-2
SORA_SIZE=1280x720
SORA_SECONDS=12
OPTIONS_MODEL=gpt-5-mini

# Debug logging for LLM options generation (set to 1 to enable)
#DEBUG_OPTIONS=1
